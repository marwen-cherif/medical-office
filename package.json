{
  "name": "cabinet-crm-monorepo",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "node scripts/generate-build-info.js && turbo build && node scripts/sign-installer.js",
    "test": "turbo test",
    "test:coverage": "turbo test:coverage",
    "lint": "turbo lint",
    "format": "turbo format"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "packageManager": "pnpm@11.9.0"
}
