## 1. Schéma & migration (crm/db.py)

- [x] 1.1 Ajouter à `_SCHEMA` la table `plans_traitement (id INTEGER PRIMARY KEY AUTOINCREMENT, patient_id INTEGER NOT NULL REFERENCES patients(id) ON DELETE CASCADE, titre TEXT NOT NULL, notes TEXT, created_at TEXT NOT NULL DEFAULT (datetime('now')))` + index sur `patient_id`.
- [x] 1.2 Ajouter `prestations (id, patient_id NOT NULL REFERENCES patients(id) ON DELETE CASCADE, plan_id REFERENCES plans_traitement(id) ON DELETE SET NULL, acte_id INTEGER, libelle TEXT NOT NULL, montant REAL NOT NULL DEFAULT 0, montant_regle REAL NOT NULL DEFAULT 0, statut TEXT NOT NULL DEFAULT 'en_attente', date_acte TEXT, dents TEXT, note TEXT, sort_order INTEGER NOT NULL DEFAULT 0, created_at TEXT NOT NULL DEFAULT (datetime('now')))` + index sur `patient_id` et `plan_id`. **Pas de colonne `type`** (un contrôle = acte à `montant = 0`). `dents`/`note` nullable. `ON DELETE SET NULL` du `plan_id` réalise le **détachement** des actes à la suppression d'un plan (D8).
- [x] 1.3 Ajouter `prestation_reglements (id, prestation_id INTEGER NOT NULL REFERENCES prestations(id) ON DELETE CASCADE, montant REAL NOT NULL, mode TEXT, date_reglement TEXT, created_at TEXT NOT NULL DEFAULT (datetime('now')))` + index sur `prestation_id` et `date_reglement` (calque de `depense_reglements`).
- [x] 1.4 Bumper `SCHEMA_VERSION` de 9 à 10. Aucune étape `_migrate()` de transformation requise (tables neuves créées par `_SCHEMA`) ; vérifier le snapshot pré-migration sur une base v9.
- [x] 1.5 Confirmer le respect des règles de préservation (additif, non destructif ; `paiements`/`documents` non touchées).

## 2. Couche d'accès aux données (crm/repo.py)

- [x] 2.1 Dataclasses `PlanTraitement`, `Prestation` (propriété `reste = montant - montant_regle` ; propriété `facturable = montant > 0`), `PrestationReglement` + leurs `_row_to_*`.
- [x] 2.2 CRUD plans : `create_plan`, `update_plan` (titre/notes), `list_plans(patient_id)`, `get_plan`, `delete_plan` (détache les prestations via `ON DELETE SET NULL` puis supprime le plan).
- [x] 2.3 CRUD prestations : `create_prestation` (validation libellé non vide, `montant ≥ 0` ; snapshot `libelle`/`montant`/`acte_id` issus de l'appelant ; `dents` normalisées « n° séparés par virgule » ; `note` libre), `update_prestation`, `list_prestations(patient_id, plan_id=...)` (et actes isolés `plan_id IS NULL`), `get_prestation`, `delete_prestation` (refus si des règlements existent — cf. 2.5).
- [x] 2.4 `add_prestation_reglement(conn, prestation_id, montant, mode=None, date_reglement=None)` : **calque** de `add_depense_reglement` — insère le versement, incrémente `montant_regle`, recalcule `statut` (`en_attente`/`regle_partiellement`/`regle`), refuse un montant ≤ 0 ou > reste. `list_prestation_reglements(prestation_id)`.
- [x] 2.5 Garde de suppression : `prestation_has_reglements(prestation_id)` ; `delete_prestation` lève/retourne une erreur si des règlements existent (D8).
- [x] 2.6 Totaux dérivés d'un plan : `plan_totaux(conn, plan_id) -> (du, encaisse, reste)` (SUM `montant`, SUM `montant_regle`) — les actes à `montant = 0` sont naturellement neutres.
- [x] 2.7 `list_prestations_a_regler(conn, patient_id)` : prestations facturables au `reste > 0` (pour le récap « à régler »), exclut les actes à `montant = 0`.
- [x] 2.8 Helper de normalisation des dents (`normalize_dents`) : découpe la saisie, nettoie, recompose en « 26, 27 » (validation FDI **non bloquante**).
- [x] 2.9 **Lecture unifiée « à recouvrer »** (D7) : `list_creances(search, date_from, date_to, limit, offset)` + `count_creances(...)` + `total_creances(...)` — **union** des `paiements` en_attente (créance de note) et des `prestations` au `reste > 0` (créance d'acte), normalisée en lignes (patient, libellé, montant/reste, nature `note|acte`, date, id source). Tri/recherche cohérents avec `list_paiements_filtered`.
- [x] 2.10 **Lecture unifiée « encaissé »** (D7) : agrégat trésorerie combinant `paiements` encaissés et `prestation_reglements` (par date), pour les totaux « encaissé » de l'écran Finances et du tableau de bord.
- [x] 2.11 **Cascade de règlement** (D9 révisé) : `creances_patient(patient_id, include_notes)` (créances ordonnées du plus ancien au plus récent) + `regler_creances(patient_id, montant, mode, date, include_notes)` qui répartit un montant unique en **paiement partiel** via `add_prestation_reglement` et renvoie `{alloue, reste, lignes}`. **L'UI appelle `include_notes=False`** : la cascade ne porte que sur les actes (fractionnables) ; les notes binaires sont exclues pour ne pas bloquer un reliquat (réglées à part via « Encaisser »).
- [x] 2.12 **Historique unifié + solde fiche** (D14 révisé) : `list_encaissements_patient` / `count_encaissements_patient` (union versements d'actes + notes encaissées, récents d'abord) et `solde_patient(patient_id) -> (du, encaisse, reste)` consolidé actes + notes.

## 3. Interface — fiche patient (crm/app.py)

- [x] 3.1 Section « Plans & actes » dans `show_patient_detail`, entre Documents et Règlements : **toutes les créances regroupées** — notes en attente, actes isolés, puis chaque plan en section repliable (patron `ExpansionTile` de `_grouped_docs_column`). En-tête : boutons « Plan », « Acte », « Régler » (cascade, affiché dès qu'il reste à recouvrer).
- [x] 3.2 `_prestation_row(p)` : libellé, date, dents (badges), montant ; si `p.facturable`, **barre de progression** `ft.ProgressBar(value=montant_regle/montant)` + « {réglé} / {montant} · reste {reste} » + chip de statut (`_DEPENSE_STATUT_LABELS`) + bouton « Régler » si `reste > 0` ; si `montant = 0`, badge **dérivé** « non facturable », sans barre. Note affichée en sous-ligne si présente.
- [x] 3.3 En-tête de plan : titre + totaux dérivés (`plan_totaux`) via `_money_summary` + **barre de progression** d'ensemble ; actions « + acte », « Supprimer le plan » (détachement). Un seul type d'ajout (pas de bouton « contrôle » distinct).
- [x] 3.4 **Composant réutilisable `_acte_card(...)`** : libellé + prix + date + **dents** (chips, ajout/suppression, `normalize_dents`) + **note** (multi-lignes) + sélecteur référentiel (`repo.list_actes`) qui **pré-remplit** libellé+prix (modifiables). **Aucun champ « type ».**
- [x] 3.5 `_plan_dialog(plan=None)` (composer) : titre + notes + **pile de cartes `_acte_card`** scrollable, bouton unique « + Acte », **total dû recalculé en direct**, suppression par carte (🗑️). Sert création et édition. Audit `log_audit`.
- [x] 3.6 `_acte_dialog(...)` : une seule `_acte_card` + sélecteur de **plan** (« aucun » par défaut) pour un acte isolé ou rattaché.
- [x] 3.7 `_regler_dialog(patient)` (D9 révisé) : **règlement global en cascade sur les actes** — un seul champ « Montant reçu » (pré-rempli au total des actes à recouvrer) + mode + date, **aperçu de la répartition en direct** (du plus ancien au plus récent), reliquat non affecté signalé (vrai trop-perçu uniquement), appelle `regler_creances(include_notes=False)`. Bouton « Régler » affiché ssi des actes restent dus. Plus `_payer_acte_dialog(prestation_id)` : **versement ciblé sur un acte** (action par ligne et depuis Finances), garde « versement > reste ».
- [x] 3.8 Confirmations de suppression (acte / plan) respectant les gardes (D8) ; ergonomie clavier cohérente (Ctrl+N, Ctrl+S/Ctrl+Entrée, sélection auto).
- [x] 3.9 **Pas de double-comptage** (D13 révisé ²) : créer un acte facturable ne crée **pas** de `paiement`, et la **génération de document ne crée plus AUCUN paiement** — la case « Créer un paiement en attente » et le champ montant sont **entièrement retirés** de `_generate_dialog`. Le suivi du dû passe exclusivement par les actes.
- [x] 3.11 **Note d'honoraires dédiée** (D15) : bouton « Note d'honoraires » sur la fiche → `_note_dialog` → `_generate_dialog(p, category=<catégorie configurée>)` ne proposant que les modèles de cette catégorie ; la génération générique les **exclut**. Réglage `note_honoraire_categorie` (clé `NOTE_CAT_KEY`) choisi via `_note_category_selector` dans Paramétrage › Modèles. Si non configurée, le bouton invite à la définir.
- [x] 3.10 **Bloc « Règlements » unifié** (D14 révisé) : remplacer l'ancien bloc « Paiements » de la fiche par un récap `_money_summary` (Dû / Encaissé / Reste via `solde_patient`) + l'**historique des règlements** (`_encaissement_row` sur `list_encaissements_patient`, paginé) — versements d'actes et notes encaissées au même endroit, synchronisé avec les lignes d'actes.

## 4. Interface — Finances : vue unifiée des créances (crm/app.py)

- [x] 4.1 Adapter `show_paiements` / `_refresh_paiements` (Finances › Paiements) pour lister les **créances unifiées** (`list_creances`, 2.9) : paiements en attente **et** actes au reste positif, au même endroit.
- [x] 4.2 Action par ligne selon la nature : « Encaisser » pour un paiement (flux existant), « Régler » pour un acte (ouvre le versement en place / `_regler_dialog` centré sur l'acte).
- [x] 4.3 Totaux de l'écran (« à recouvrer », « encaissé ») via `total_creances` / l'agrégat 2.10 : **inclure les deux sources**. Idem KPI du tableau de bord (`show_dashboard`, ex-`total_paiements`).
- [x] 4.4 Préserver l'affichage et le comportement des paiements existants (ils restent visibles et fonctionnels) ; renommage éventuel de l'onglet (« Créances ») = cosmétique.

## 5. Vérification (manuelle, sur copie d'une base de production)

- [x] 5.1 Créer un plan via le composer (plusieurs cartes d'actes pré-remplies depuis le référentiel + une « Consultation de contrôle » à 0) ; vérifier le snapshot libellé/prix, le total en direct, et le badge « non facturable » de l'acte à 0.
- [x] 5.2 Ajouter un acte **isolé** (sans plan) ; vérifier son affichage et la possibilité de le régler. Vérifier qu'**aucun paiement** n'est créé par l'ajout d'un acte (D13).
- [x] 5.3 Saisir des **dents** (26 puis 27 → « 26, 27 ») et une **note** ; vérifier l'affichage sur la fiche ; vérifier qu'un acte sans dents/note reste valide.
- [x] 5.4 Régler partiellement un acte (D9 révisé : via la **cascade globale** « Régler » et/ou « Régler cet acte ») : vérifier reste partiel, statut « réglé partiellement », barre ; refuser un versement > reste ; solder l'acte. *(Le « dialogue récap ligne par ligne » initial a été remplacé par la cascade ; partiel + reste à payer vérifiés en direct.)*
- [x] 5.5 Vérifier les totaux et la barre de progression d'un plan (dû / encaissé / reste).
- [x] 5.6 **Vue Finances unifiée** : pour un patient ayant un paiement en attente (note) et un acte non soldé, vérifier que les deux apparaissent au même endroit avec le bon bouton chacun, et que le **total à recouvrer additionne les deux** (idem KPI tableau de bord).
- [x] 5.7 Modifier le prix d'un acte dans le référentiel et vérifier que les prestations déjà créées **ne changent pas** (snapshot).
- [x] 5.8 Supprimer un plan contenant des actes réglés : vérifier que les actes deviennent isolés et que les règlements sont conservés ; vérifier le refus de suppression d'un acte portant des règlements.
- [ ] 5.9 **(GATE MANUEL RESTANT)** Vérifier que les documents/paiements existants restent intacts après mise à niveau (chargement d'une `cabinet.db` de production + snapshot pré-migration présent). Non couvert par les tests headless ; à faire sur une copie réelle avant livraison.
