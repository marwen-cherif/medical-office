## 1. Composants UI Atomiques

- [x] 1.1 Ajouter la classe `whitespace-nowrap` au composant `Badge` dans `apps/web/src/components/ui/badge.tsx`
- [x] 1.2 Ajouter la classe `whitespace-nowrap` au composant `Label` dans `apps/web/src/components/ui/label.tsx`
- [x] 1.3 Ajouter la classe `whitespace-nowrap` au composant `DropdownMenuLabel` dans `apps/web/src/components/ui/dropdown-menu.tsx`
- [x] 1.4 Créer le composant `ClickToCopy` dans `apps/web/src/components/ui/click-to-copy.tsx` avec gestion de l'état copié et icône temporaire

## 2. Intégration dans les Fiches Détails

- [x] 2.1 Intégrer `ClickToCopy` dans `IdRow` de `apps/web/src/screens/PatientDetail.tsx` pour copier email, téléphone, date de naissance et adresse
- [x] 2.2 Intégrer `ClickToCopy` dans `Row` de `apps/web/src/screens/PrestataireDetail.tsx` pour copier email, téléphone, adresse et notes

## 3. Validation

- [x] 3.1 Vérifier la compilation et le typage du projet avec les nouvelles modifications
- [x] 3.2 Valider le fonctionnement de la copie dans le presse-papiers et le changement visuel d'icône
